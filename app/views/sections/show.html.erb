<h2><%= @section.name %><div class="h5"><%= link_to "Edit Section Name", edit_section_path(@section.id) %></div></h2>		
	<strong># of Students:</strong> <%= @students.count %>
<br/><br />
<div class="table-responsive">
<table class="table table-hover">
	<tr>
		<th>First Name</th>
		<th>Last Name</th>
		<th>Emergency Contact</th>
		<th>Emergency Phone #</th>
		<th>&nbsp;</th>
	</tr>
<% @students.each do |student| %>
<% @students = Student.where(section_id: section.id).count %>
	<tr>
		<td><%= student.first_name %></td>
		<td><%= student.last_name %></td>
		<td><%= student.emerg_contact_name %></td>
		<td><%= student.emerg_contact_phone %></td>
		<td><%= link_to "View", student_path(student.id), class: "btn btn-info btn-sm" %>&nbsp;&nbsp;&nbsp;<%= link_to "Edit", edit_student_path(student.id), class: "btn btn-warning btn-sm" %>&nbsp;&nbsp;&nbsp;<%= link_to "Delete", student_path(student.id)	, method: 'delete', confirm: "Are You Sure?", class: "btn btn-danger btn-sm" %></td>
	</tr>
	<% end %>
